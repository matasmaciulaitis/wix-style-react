import AddressInputOption from './AddressInputOption';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(AddressInputOption);

metadata.exportInfo = {
  path: 'src/AddressInputOption/AddressInputOption.js',
};
